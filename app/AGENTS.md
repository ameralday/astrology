# Expo HAS CHANGED

This project runs Expo SDK 54 (downgraded from the initial SDK 57 scaffold to match the test
device's Expo Go client - see root `CLAUDE.md`). Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code, and don't bump the `expo`
package to a newer SDK without confirming Expo Go client compatibility first.
